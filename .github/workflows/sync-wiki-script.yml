name: Sync Wiki via simple script
on:
  push:
    branches: [ "main" ]
    paths:
      - "wiki/**"
      - "scripts/update_wiki_simple.py"
      - ".github/workflows/sync-wiki-script.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          echo "📦 Installing Python dependencies..."
          if [ -f requirements.txt ]; then 
            echo "📋 Installing from requirements.txt..."
            pip install -r requirements.txt
          else
            echo "ℹ️ No requirements.txt found, skipping dependency installation"
          fi

      - name: Run simple wiki sync script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
          GITHUB_ACTIONS: "true"
        run: |
          set -e
          echo "🚀 Running simple wiki sync script..."
          echo "📋 Repository: $REPO"
          echo "🔑 Commit SHA: $SHA"
          echo "🌐 Environment: GitHub Actions"
          
          # Run the simple script with timeout
          timeout 60 python scripts/update_wiki_simple.py
          
          if [ $? -eq 0 ]; then
            echo "✅ Simple wiki sync script completed successfully"
          else
            echo "⚠️ Script may have timed out or encountered issues"
          fi

      - name: Verify script execution
        run: |
          echo "🔍 Verifying script execution..."
          if [ -f "wiki/Home.md" ]; then
            echo "✅ Wiki file created/updated successfully"
            echo "📁 Wiki directory contents:"
            ls -la wiki/
            echo ""
            echo "📄 Wiki file preview (first 10 lines):"
            head -10 wiki/Home.md
          else
            echo "❌ Wiki file not found after script execution"
            exit 1
          fi
          
          echo "✅ Script execution verification complete"
          echo "📚 Check the GitHub Wiki for updates"
